# Slice 1 has zero runtime dependencies (stdlib only).
# Dev-only:
pytest>=7.0

# Slice 2a — pre-trained NER (models downloaded separately, see README)
# spaCy 3.7.x is the last line supporting Python 3.9; newer needs Python >=3.10.
spacy>=3.7,<3.8
camel-tools

# Slice 2b — native PDF read/redact
pymupdf

# Slice 4 — web API + audit DB
fastapi
uvicorn[standard]
python-multipart
sqlalchemy
psycopg2-binary   # PostgreSQL driver (Docker runtime)
httpx             # dev: FastAPI TestClient
